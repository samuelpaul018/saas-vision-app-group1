# app

## Deployed application link

Add your live Google App Engine URL here.

Example:

- https://YOUR_PROJECT_ID.uc.r.appspot.com

## What the app does

The application allows a user to upload an image from the browser. The Node.js + Express server receives the image, sends the image data to the Google Cloud Vision Label Detection service, and returns a formatted HTML page listing the detected labels, confidence scores, and topicality scores.
