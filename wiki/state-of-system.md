# state of system

## Current status

Update this section based on your final test results.

### Example fully working status
The system is fully working. Users can upload an image from the browser, the server successfully sends the image to Google Cloud Vision Label Detection, and the app returns a formatted list of labels in the browser. The application has also been deployed to Google App Engine and tested successfully.

### Example issues section
If anything is not working, list it clearly here. For example:

- deployment works, but some large image files exceed the upload size limit
- local testing works, but App Engine service account permissions still need adjustment
- UI is functional, but the team still needs to add final screenshots to the wiki
